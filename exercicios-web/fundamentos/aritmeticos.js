const [a, b, c, d] = [3, 5, 1, 15] // destructuring

const soma = a + b + c + d

const subtracao = d - b

const multiplicacao = a * b

const divisao = d / a

const modulo = a % 2// se o resto for 0 é par, se for 1 é impar

console.log(soma, subtracao, multiplicacao, - divisao /*operador unario usando o negativo*/, modulo)